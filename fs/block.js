

class Block {
    constructor (keys, data) {
        this.keys = keys
        this.data = data
    }
    
    publish () {
        // Create transaction
        // Push transaction to the network
    }
}

